{
  "name": "@averybiskup/avery-cmp",
  "version": "1.0.4",
  "scripts": {
    "build": "rm -rf dist && NODE_ENV=production babel src --out-dir dist --copy-files"
  },
  "devDependencies": {
    "@babel/cli": "^7.21.0",
    "@babel/preset-env": "^7.21.4",
    "ts-loader": "^9.4.2",
    "typescript": "^5.0.4"
  }
}
